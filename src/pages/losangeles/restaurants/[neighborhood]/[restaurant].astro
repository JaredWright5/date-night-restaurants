---
// Redirect old restaurant URL structure to new one
export function getStaticPaths() {
  return [];
}

const { neighborhood, restaurant } = Astro.params;
return Astro.redirect(`/restaurants/${neighborhood}/${restaurant}/`, 301);
---